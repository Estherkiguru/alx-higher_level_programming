#!/usr/bin/python3
"""module for same class or inherit from"""


def is_kind_of_class(obj, a_class):
    """check if obj is an instance 0f or inherits from a_class"""
    return isinstance(obj, a_class)
